import { describe, expect, test } from 'vitest';

describe('App', () => {

    test('should ', () => {

        const cadena = 'Esto es una cadena'
        expect(cadena).toBe('Esto es una cadena');
    })

})
